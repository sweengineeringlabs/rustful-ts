[package]
name = "pipeline-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types and utilities for data transformation pipelines"

[dependencies]
pipeline-spi = { path = "../pipeline-spi" }
thiserror = { workspace = true }
