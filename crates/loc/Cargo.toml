[package]
name = "loc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Fast, lightweight lines of code counter"

[[bin]]
name = "loc"
path = "src/main.rs"

[dependencies]
# Zero dependencies - std only
