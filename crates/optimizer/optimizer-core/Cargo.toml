[package]
name = "optimizer-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Optimizer Core - implementations of optimization algorithms"

[dependencies]
optimizer-spi = { path = "../optimizer-spi" }
optimizer-api = { path = "../optimizer-api" }
indicator-core = { path = "../../indicator/indicator-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
rand_distr = "0.4"
rayon = "1.10"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "optimizer_bench"
harness = false
