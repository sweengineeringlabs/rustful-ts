[package]
name = "optimizer-spi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Optimizer Service Provider Interface - traits and base types"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
