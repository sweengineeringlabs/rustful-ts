[package]
name = "tuning-facade"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hyperparameter tuning Facade - unified re-exports"

[features]
default = []
parallel = ["tuning-core/parallel"]

[dependencies]
tuning-spi = { path = "../tuning-spi" }
tuning-api = { path = "../tuning-api" }
tuning-core = { path = "../tuning-core" }
