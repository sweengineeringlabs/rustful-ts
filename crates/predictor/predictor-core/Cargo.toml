[package]
name = "predictor-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types and utilities for time series predictors"

[dependencies]
predictor-spi = { path = "../predictor-spi" }
thiserror = { workspace = true }
