[package]
name = "indicator-pattern"
description = "Pattern recognition indicators - ZigZag, Heikin Ashi, Candlestick patterns, etc."
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
simd = []

[dependencies]
indicator-spi = { path = "../indicator-spi" }
indicator-api = { path = "../indicator-api" }
serde.workspace = true

[dev-dependencies]
