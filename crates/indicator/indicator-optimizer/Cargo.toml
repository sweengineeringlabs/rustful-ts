[package]
name = "indicator-optimizer"
version = "0.1.0"
edition = "2021"
description = "Indicator parameter optimization for technical analysis"
license = "MIT OR Apache-2.0"
repository = "https://github.com/rustful-ts/rustful-ts"
keywords = ["trading", "indicators", "optimization", "backtesting"]
categories = ["finance", "algorithms"]

[dependencies]
indicator-core = { path = "../indicator-core" }
indicator-spi = { path = "../indicator-spi" }
rand = "0.8"
rayon = "1.10"

[dev-dependencies]
criterion = "0.5"

# [[bench]]
# name = "optimizer"
# harness = false
