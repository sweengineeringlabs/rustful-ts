[package]
name = "indicator-oscillators"
description = "Technical indicator oscillators - ROC, Momentum, Chande, KST, PPO, etc."
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
simd = []

[dependencies]
indicator-spi = { path = "../indicator-spi" }
indicator-api = { path = "../indicator-api" }
serde.workspace = true

[dev-dependencies]
